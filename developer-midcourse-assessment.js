(()=>{
const cfg=window.ELRP_MIDCOURSE_ASSESSMENT;if(!cfg)return;
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
function css(){if(document.querySelector('link[data-midcourse-css]'))return;const l=document.createElement('link');l.rel='stylesheet';l.href='midcourse-assessment.css?v=20260910-app1';l.dataset.midcourseCss='1';document.head.appendChild(l)}
function render(){
 const workspace=document.querySelector('.developer-workspace');if(!workspace)return;
 const title=workspace.querySelector('.workspace-title h1');
 if(!title||!title.textContent.includes('Mid-Course'))return;
 const body=workspace.querySelector('.native-workspace-body');if(!body||body.querySelector('#developerMidcourseApplication'))return;
 css();title.textContent='Mid-Course Leadership Application Assessment';
 const groups={};cfg.questions.forEach((q,i)=>{(groups[q.week]??=[]).push({...q,index:i})});
 body.innerHTML=`<div id="developerMidcourseApplication" class="midcourse-application"><section class="native-lesson-hero"><span>Developer inspection · 70% application assessment</span><h2>Mid-Course Leadership Application Assessment</h2><p>12 executive scenarios assess demonstrated judgement across Weeks 1–3. The three written evidence reflections are required but ungraded. Correct answers remain server-side and are not exposed in Developer Mode.</p></section><div class="midcourse-overview"><div><strong>12</strong><span>Scored application scenarios</span></div><div><strong>70%</strong><span>Assessment weighting</span></div><div><strong>80%</strong><span>Overall stage pass mark</span></div></div>${Object.entries(groups).map(([wk,qs])=>`<section class="native-lesson-card midcourse-section"><div class="midcourse-section-head"><div><span>${esc(wk)}</span><h3>${wk==='Week 1'?'Leadership Identity & Confidence':wk==='Week 2'?'Executive Presence & Personal Authority':'Assertiveness & Difficult Conversations'}</h3></div><small>4 scenarios</small></div>${qs.map(q=>`<article class="midcourse-question"><div class="midcourse-qmeta"><span>${esc(q.week)}</span><span>${esc(q.competency)}</span></div><strong>${q.index+1}. ${esc(q.prompt)}</strong><div class="midcourse-options">${q.options.map((o,oi)=>`<label class="midcourse-option"><input type="radio" name="dev-mid-${q.index}" value="${oi}"><span>${esc(o)}</span></label>`).join('')}</div></article>`).join('')}</section>`).join('')}<section class="native-lesson-card"><div class="native-section-head"><span>Evidence of change · ungraded</span><h2>Required written reflection</h2></div><div class="midcourse-evidence">${cfg.reflections.map(r=>`<label><strong>${esc(r.label)}</strong><small>${esc(r.prompt)}</small><textarea placeholder="Developer inspection only — nothing is submitted"></textarea></label>`).join('')}</div></section><div class="danger-note"><strong>Developer safety:</strong> This inspection view does not submit an assessment, consume an attempt, issue credentials, or change participant records.</div></div>`;
}
let t;new MutationObserver(()=>{clearTimeout(t);t=setTimeout(render,40)}).observe(document.body,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',render);else render();
})();
